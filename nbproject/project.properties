annotation.processing.enabled=true
annotation.processing.enabled.in.editor=false
annotation.processing.processors.list=
annotation.processing.run.all.processors=true
application.desc=This program helps you manage your wildlife sightings.
application.homepage=http://www.mywild.co.za
application.splash=wildlog/resources/icons/SplashScreen.gif
application.title=WildLog
application.vendor=MyWild (Henry de Lange)
auxiliary.org-netbeans-spi-editor-hints-projects.perProjectHintSettingsFile=nbproject/cfg_hints.xml
build.classes.dir=${build.dir}/classes
build.classes.excludes=**/*.java,**/*.form
# This directory is removed when the project is cleaned:
build.dir=build
build.generated.dir=${build.dir}/generated
build.generated.sources.dir=${build.dir}/generated-sources
# Only compile against the classpath explicitly listed here:
build.sysclasspath=ignore
build.test.classes.dir=${build.dir}/test/classes
build.test.results.dir=${build.dir}/test/results
debug.classpath=\
    ${run.classpath}
debug.test.classpath=\
    ${run.test.classpath}
# This directory is removed when the project is cleaned:
dist.dir=dist
dist.jar=${dist.dir}/WildLog.jar
dist.javadoc.dir=${dist.dir}/javadoc
endorsed.classpath=
excludes=
includes=**
jar.archive.disabled=${jnlp.enabled}
jar.compress=false
jar.index=${jnlp.enabled}
javac.classpath=\
    ${libs.EXIF.classpath}:\
    ${libs.H2.classpath}:\
    ${libs.JMF.classpath}:\
    ${libs.MediaUtil.classpath}:\
    ${libs.OpenMap.classpath}:\
    ${libs.SwingX-WS.classpath}:\
    ${libs.SwingX.classpath}:\
    ${libs.absolutelayout.classpath}:\
    ${libs.swing-app-framework.classpath}:\
    ${reference.Astro.jar}:\
    ${reference.GpxReader.jar}:\
    ${reference.KmlGenerator.jar}:\
    ${reference.WildLogDataCore.jar}
# Space-separated list of extra javac options
javac.compilerargs=
javac.deprecation=true
javac.processorpath=\
    ${javac.classpath}
javac.source=1.7
javac.target=1.7
javac.test.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}:\
    ${libs.junit_4.classpath}
javadoc.additionalparam=
javadoc.author=false
javadoc.encoding=${source.encoding}
javadoc.noindex=false
javadoc.nonavbar=false
javadoc.notree=false
javadoc.private=false
javadoc.splitindex=true
javadoc.use=true
javadoc.version=false
javadoc.windowtitle=
jaxbwiz.endorsed.dirs="${netbeans.home}/../ide12/modules/ext/jaxb/api"
jnlp.codebase.type=local
jnlp.codebase.url=file:/C:/Documents%20and%20Settings/Henry/My%20Documents/NetBeansProjects/WildLog/dist/
jnlp.descriptor=application
jnlp.enabled=false
jnlp.mixed.code=default
jnlp.offline-allowed=false
jnlp.signed=false
jnlp.signing=
jnlp.signing.alias=
jnlp.signing.keystore=
# Property libs.absolutelayout.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.absolutelayout.classpath=../../../../../Program Files/NetBeans 6.0/java1/modules/ext/AbsoluteLayout.jar
# Property libs.SwingX.classpath is set here just to make sharing of project simpler.
# The library definition has always preference over this property.
libs.SwingX.classpath=../../../../WildLog/Utils/swingx-0.9.3.jar
main.class=wildlog.WildLogApp
# Optional override of default Codebase manifest attribute, use to prevent RIAs from being repurposed
manifest.custom.codebase=
# Optional override of default Permissions manifest attribute (supported values: sandbox, all-permissions)
manifest.custom.permissions=
manifest.file=manifest.mf
meta.inf.dir=${src.dir}/META-INF
mkdist.disabled=false
platform.active=default_platform
project.Astro=../Astro
project.GpxReader=../GpxReader
project.KmlGenerator=../KmlGenerator
project.WildLogDataCore=../WildLogDataCore
reference.Astro.jar=${project.Astro}/dist/Astro.jar
reference.GpxReader.jar=${project.GpxReader}/dist/GpxReader.jar
reference.KmlGenerator.jar=${project.KmlGenerator}/dist/KmlGenerator.jar
reference.WildLogDataCore.jar=${project.WildLogDataCore}/dist/WildLogDataCore.jar
run.classpath=\
    ${javac.classpath}:\
    ${build.classes.dir}
run.test.classpath=\
    ${javac.test.classpath}:\
    ${build.test.classes.dir}
source.encoding=UTF-8
src.dir=src
test.src.dir=test
